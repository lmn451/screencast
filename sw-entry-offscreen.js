/**
 * Clean Entry Point for Offscreen Document
 * offscreen.js has no exports - it runs immediately on import
 * We just need to import it to trigger the module's side effects
 */
import './offscreen.js';
